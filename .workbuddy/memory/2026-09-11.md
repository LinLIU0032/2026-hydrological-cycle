# 2026-09-11 工作日志

## PROD-W1-WORKBUDDY — Lecture 16 Slides 21 / 22 / 24（完成）

- 合同：`planning/TASK_PROD_W1_WORKBUDDY.md`；唯一输出目录 `sections/PROD-W1-WORKBUDDY/`。
- 交付：`L16_W1_WorkBuddy_Slides21-22-24_v01.pptx`（3 页原生可编辑、3 页讲者备注）、同名 `.pdf`（3 页 960×540pt、文字可选中）、`previews/*.png`（由交付 PPTX 渲染）、`PROD-W1-WORKBUDDY_COMPLETION.md`。
- 工具链：ppt-master v6.3.1（`planning/tooling/ppt-master-v6.3.1/...`），Python `planning/tooling/ppt-master-py312-v6.3.1/Scripts/python.exe`；attribution guard 退出 0；final 质量门 3/3 通过 0 警告；`svg_to_pptx.py` POSTFLIGHT passed。
- 关键工程结论（后续复用）：
  1. `project_manager.py init` 会给项目名追加 `_<format>_<YYYYMMDD>`；合同要求固定目录名时用 `--dir <父目录>` 初始化后重命名目录，并在 `validation/workflow.log` 追加手工恢复说明。
  2. 本机 PowerPoint COM：`New-Object -ComObject PowerPoint.Application` 会因 `TYPE_E_CANTLOADLIBRARY`（Office Interop 类型库加载失败）失败；改用 `[Type]::GetTypeFromProgID("PowerPoint.Application")` + `[Activator]::CreateInstance()` 晚绑定即可正常 `SaveAs(pdf, 32)`、`Slide.Export(png, "PNG", w, h)`。
  3. PowerShell 工具会吞掉子进程 stdout，结果必须 `Out-File` 到临时文件后用 Read 读回。
  4. Bash 工具禁止调用 PowerShell；pwsh / COM 一律走 PowerShell 工具。
  5. `image_treat.py` 无裁剪能力，图件裁剪只能在 SVG 侧用 `preserveAspectRatio="xMidYMin slice"` 实现，并在 §VIII / spec_lock 声明 `crop=adaptive`。
  6. PowerPoint 导出 PDF 后文字可选中，是"原生文本而非轮廓"的现成证据。
- 内容决策：Slide 22 只用 UE Fig.12.19（C-01）单一数据集，四级储库 830/500/1500/38000 Gt C，沉积物与岩石圈标明"图中未给出总量"；Slide 24 用原生对齐矩阵（共享左侧 rail：储库/通量/停留时间），停留时间只给"最快/最慢"储库名，不建数值时间尺度表，指回第 08 页表 3-1 与教材图 4-15/4-16。
- 未决事项：`exports/` 内残留重复 PPTX 与 `.pptx-build-*` 暂存目录（未删除，等用户确认）；实时预览服务运行在 `http://127.0.0.1:6060`（pid 33716）。

## PROD-W1-WORKBUDDY r1 — GATE 6 复议修订（完成）

- 触发：`qa/GATE6_WAVE1_ACCEPTANCE_20260911.md`，仅 Slide 22 被 REVISION REQUIRED（Slides 21/24 APPROVED）。
- 缺陷：面板印着"自小而大排序"，却把 大气830 排在 生物圈500 之前，还把大气写成"量级最小的储库"；讲者备注同错。
- 修订（只改 `svg_output/22_carbon_reservoirs.svg` + 两处备注）：
  1. 四个数值储库改为严格升序 1 生物圈500 → 2 大气830 → 3 土壤1500 → 4 海洋38,000 Gt C。
  2. 删除"量级最小的储库"，大气副标题改为纯描述"大气中 CO₂ 所含的碳"。
  3. 沉积物/岩石圈拆成独立定性组：分隔线标签"地质储库 | Geological reservoirs · 定性描述，不参与上方数值排序"，序号 5/6 取消改为空心圆，逐行标"图中未给出总量"。理由：C-01 只印四个总量，对沉积物/岩石圈不印，编入同一升序序列等于替源图断言它没说的顺序。
  4. 备注改为"一、生物圈五百；二、大气八百三十；三、土壤一千五百；四、海洋三万八千"，并明确"大气并不是最小的那一个"。
- 未改动（已核验）：Slides 21/24 的 `svg_output` SHA-256 与 r0 完全一致；Slide 22 标题、教学句、C-01 图与裁剪、来源脚注、页码、画布、配色、字阶全部保持。
- 重生成链：attribution_guard(0) → svg_quality_checker（3/3 通过，元素 57→58）→ total_md_split → finalize_svg → svg_to_pptx（POSTFLIGHT passed）→ PowerPoint 导出 PDF + 3 张预览。
- 交付物大小更新：pptx 1,870,282 B；pdf 527,204 B；预览 110,327 / 969,111 / 115,424 B。
- 新增验收证据：`validation/revision22_check.txt`（8/8 PASS：顺序、升序数值、旧串清除、地质组独立无编号、备注一致、未改元素保留、预览 1920×1080、PDF 第 2 页原生文本）。
- r0 快照保留在 `backup/20260911_pre_revision/`（pptx/pdf/22 预览/22 备注）与 `backup/20260911_112844/svg_output/`。
- 坑：`exports/.pptx-build-<hash>/pptx_content/ppt/slides/slide2.xml` 是 r0 暂存，仍含被否决的旧串，会让"全仓 grep 旧串"出现假命中——它不是交付物。

## PROD-W1-WORKBUDDY — 收尾清理（用户授权后完成，15:07）

- 预览服务：ppt-master live preview 因空闲超时**已自行退出**（pid 33716 不存在、6060 无监听）；用工具正式入口 `scripts/svg_editor/server.py <PROJ> --shutdown` 清掉遗留 `live_preview/lock.json`（输出 `live preview already stopped; cleared stale lock`）。`server.log` 保留作记录。
  - 停止方式记录：`server.py` 提供 `--shutdown`；另有 `/api/shutdown` 路由与空闲自动关闭（`idle_timeout`）。
- `exports/` 清理：删掉 `.pptx-build-38676-…/`（18 文件/19 目录）、`_112828.pptx`、`_112844.pptx`、`_145920.pptx`；保留 `_145937.pptx`（postflight 通过、与契约交付物同 md5 `8a5487b5…`）。
  - `.pptx-build-11468-…/`（r0 暂存）动手前已被工具自身 safe-delete 清掉，故旧串 grep 命中消失。
  - **更正**：早前报告称 `_112828` 与 `_112844` "字节相同"不成立——md5 分别为 `0b5b1cb9…` / `8eefac80…`，只是字节数相同。四个 PPTX 的 md5 两两不同。
- 删除方式（遵守"禁止批量递归删除"约束）：用 Python 逐文件 `os.remove` + 自底向上 `os.rmdir`，脚本内置守卫——路径必须落在 `exports/` 内、且不得命中保留文件名，逐条打印被删路径。**未使用** `rm -rf` / `Remove-Item -Recurse` / `del /s` / `shutil.rmtree`。
- 日志：`validation/cleanup_20260911.txt`；完成报告新增 `HOUSEKEEPING` 章节，原两个待确认项标记为 Resolved，仅余 C-01 `crop=adaptive` 一项 Open。
- 清理后复核：exports/ 仅 1 文件；无 `.pptx-build-*` 残留；交付 pptx 3 页+3 备注、pdf 3 页第 2 页原生文本 532 字符，均正常。

## PROD-W1-WORKBUDDY — design_spec 一致性修正（15:0x）

- 发现 W1 的 `design_spec.md` §III Mode Behavior、§VIII 图件行、§IX Slide 22 内容大纲仍写着 r0 的"六级层级、大气在前"，与 r1 交付页矛盾。属 r1 遗漏（当时只改了 SVG 与备注）。
- 已在原文件就地改正三处，使规格与交付页一致；未触碰任何幻灯片内容。此改动超出 r1 修订请求范围，已在 W2 完成报告的 QUESTIONS 中说明。

## PROD-W2-WORKBUDDY — Lecture 16 Slides 25 / 26（完成）

- 合同：`planning/TASK_PROD_W2_WORKBUDDY.md`；门位：GATE 7（目标 18/35）；唯一输出目录 `sections/PROD-W2-WORKBUDDY/`。
- 交付：`L16_W2_WorkBuddy_Slides25-26_v01.pptx`（677,279 B，2 页原生可编辑 + 2 页备注）、同名 `.pdf`（337,020 B，2 页 960×540pt 文字可选中）、`previews/*.png`（1920×1080，由交付 PPTX 渲染）、`PROD-W2-WORKBUDDY_COMPLETION.md`。
- 工具链：guard 0 → 质量门 2/2 通过 0 警告 → finalize（1 图嵌入）→ `svg_to_pptx` POSTFLIGHT passed；导出脚本与 W1 共用晚绑定 COM 方案。
- 内容决策：
  1. **Slide 25 全页不用任何数值**。理由：可选数据集互不兼容——UE 120 Gt C/yr 总交换、CN 表4-2（陆地植物 560 Gt、年生产量 56.4 Gt/a）、EP 63 Gt 明令禁用。不印数字即从结构上排除混用风险，教学落在过程与方向。
  2. Slide 25 三条命名通路（光合向下 / 呼吸向上 / 分解向上）用主体碳色实心箭头；另加一条更细、次级色 `#7A4A33` 的"枯落物 Litter"连接（植被→土壤），使环真正闭合、分解才有作用对象；它不是第四种通路。
  3. 土壤卡片画出带地平线与 8 个有机碳颗粒的土壤体，标注 `土壤碳储库 | Soil carbon`；植被卡片带 `储库 Reservoir` 芯片。不编码两者体量比，故不产生定量宣称。
  4. Slide 26 选 C-03 主选项：整图**零裁切**放入，色标 −108…+108、单位 g/(m²·a)、正负号句子全部保留；因放了 C-03，全页与备注**均不出现** UE ~80 Gt C/yr（已用探针验证）。
  5. C-03 为 865×750（比例 1.153）竖版，在 720 高画布上最大约 581 px 宽，图内色标数字仅约 9 px；故不动图，改由原生"读数规则"卡以 15 px 复述正负约定/单位/色标区间，保证可读性。
  6. Slide 25 不用水蓝（合同限定水蓝仅承载海洋含义），陆地—水连接用中性卡片渲染。
- 验收证据：`validation/w2_verification.txt`（包结构、逐页原生/栅格数、页码、正负号与单位探针、无 80 Gt 探针、C-03 完整性、PDF 原生文本、预览尺寸）。
- 坑与教训：质量门首轮报 2 条 `paragraph-like line run`，多行正文必须是单 `<text>`+`<tspan>`；中文引号改全角后不再被转义为 `&quot;`；我的核验脚本正则因 `<a:off>` 与 `<a:ext>` 间有换行而误抓分组变换（脚本缺陷非页面缺陷，已修正）。
- 未决：`exports/` 现有 4 个 pipeline PPTX + 3 个 `.pptx-build-*` 暂存（未删，待用户确认）。
